# Glitch Environment Variables
# Glitch will automatically set:
# - PROJECT_DOMAIN: your glitch app domain
# - PORT: the port to listen on
#
# No additional env vars needed for basic operation.
# Database file is stored at ./tracker.db (persists on Glitch)
